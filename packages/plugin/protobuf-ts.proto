syntax = "proto3";

option go_package = "Protocol";
option optimize_for = CODE_SIZE;

 // 角色信息
 message Role {

    // 角色名
    string name = 1;

    // 角色等级
    string level = 2;

    // vip等级
    repeated int32 vip = 3;
}